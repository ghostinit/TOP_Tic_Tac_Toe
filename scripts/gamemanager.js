
const GameManager = (
    function () {
        const players = [createPlayer(1), createPlayer(2)];
        let gameOver = false;
        let gameActive = false;
        let playerTurn = 1;
        const board = GameBoard;
    }
)();